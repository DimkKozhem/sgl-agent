# SQL-agent

Интеллектуальная система для автоматического анализа и оптимизации структуры баз данных с использованием LLM.

## 🎯 Описание

SQL-agent — это REST API сервис для асинхронного анализа баз данных, который использует современные языковые модели для создания оптимизированных рекомендаций по структуре БД. Система анализирует DDL команды, SQL запросы и метрики производительности, возвращая конкретные предложения по улучшению.

### Что принимает система:
- **DDL команды** — схема таблиц базы данных
- **SQL запросы** — с метриками выполнения и статистикой
- **JDBC URL** — для подключения и получения метаданных

### Что возвращает:
- **Оптимизированные DDL** — с партиционированием, кластеризацией и сжатием
- **Команды миграции** — стратегия переноса данных
- **Улучшенные запросы** — с оптимизацией производительности
- **Оценка качества** — 100-балльная шкала результатов

## ⚡ Быстрый старт

```bash
# Клонирование репозитория
git clone <repository-url>
cd sql-agent

# Установка зависимостей
pip install -r requirements.txt

# Настройка API ключей
cp env_example.txt .env
nano .env  # Добавьте ваш OPEN_ROUTER API ключ

# Запуск сервера
python main.py

# Тестирование
python test.py
```

## 📋 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/health` | GET | Проверка состояния сервиса |
| `/new` | POST | Создание новой задачи оптимизации |
| `/status` | GET | Получение статуса задачи по `task_id` |
| `/getresult` | GET | Получение результата задачи |
| `/stats` | GET | Статистика работы сервиса и LLM |

### Пример создания задачи

```bash
curl -X POST "http://localhost:8001/new" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "jdbc:trino://localhost:8080?catalog=flights",
    "ddl": [
      {"statement": "CREATE TABLE flights (id INTEGER, departure VARCHAR)"}
    ],
    "queries": [
      {
        "queryid": "query-1",
        "query": "SELECT * FROM flights WHERE id = 1",
        "runquantity": 100
      }
    ]
  }'
```

## 🏗️ Архитектура

```
┌─────────────────┐
│   FastAPI       │  REST API с валидацией
│   (api.py)      │
└────────┬────────┘
         │
┌────────▼───────────┐
│  TaskManager       │  Асинхронная обработка задач
│  (task_manager.py) │  (до 4 параллельных задач)
└────────┬───────────┘
         │
┌────────▼────────────┐
│  LLMAnalyzer        │  Гибридный анализатор БД
│  (llm_analyzer.py)  │  • LLM для анализа стратегии
└─────────────────────┘  • Детерминированная генерация SQL
                         • Реальные оптимизации через AST
                         • Робастный парсинг запросов
```

### Ключевые компоненты:

**SimpleTaskManager** — управление жизненным циклом задач с таймаутами и очисткой

**LLMAnalyzer** — гибридный пайплайн оптимизации:
- LLM анализирует схему и предлагает стратегию
- Детерминированный движок генерирует валидный SQL
- Реальные оптимизации запросов через sqlglot
- Подключение к БД для получения статистики

**DatabaseConnector** — получение метаданных из Trino/PostgreSQL/MySQL

**Models (Pydantic)** — строгая валидация входных и выходных данных

## 🤖 LLM Pipeline

### Используемые модели:

| Назначение | Модель | Особенности |
|------------|--------|-------------|
| **Анализ БД** | `nvidia/nemotron-nano-9b-v2` | Анализ схемы, стратегии оптимизации |
| **Оценка качества** | `google/gemini-2.5-flash-preview-09-2025` | 100-балльная оценка результатов |

### Процесс работы:

1. **Подключение к БД** → получение реальной статистики (row_count, cardinality)
2. **LLM анализ** → определение стратегии партиционирования и кластеризации
3. **Генерация DDL** → детерминированное создание валидного SQL
4. **Создание миграций** → стратегия переноса данных с валидацией
5. **Оптимизация запросов** → реальные изменения SQL через AST
6. **Автоматическая оценка** → качество по 100-балльной шкале

### Технические особенности:

- **Гибридный подход**: LLM для анализа, шаблоны для генерации
- **Реальные оптимизации**: изменение SQL через AST, не комментарии
- **Робастный парсинг**: sqlglot с fallback на улучшенный regex
- **Параллельная обработка**: до 6 потоков для запросов
- **Строгая валидация**: полные пути `catalog.optimized.table`

## 📊 Результаты тестирования

### Протестировано 5 схем данных:

| Dataset | Таблицы | Запросы | Время (s) | DDL | Миграции | Оценка | Статус |
|---------|---------|---------|-----------|-----|----------|--------|--------|
| **flights** | 1 | 20 | 168 | 2 | 2 | 75/100 | ✅ |
| **questsH** | 37 | 10 | 297 | 38 | 76 | 86/100 | ✅ |
| **linear_schema** | 4 | 3 | 139 | 5 | 8 | 94/100 | ✅ |
| **star_schema** | 5 | 4 | 139 | 6 | 10 | 94/100 | ✅ |
| **network_schema** | 5 | 5 | 77 | 6 | 10 | 94/100 | ✅ |

**Итого**: 100% успешность, средняя оценка 89/100

### Ключевые достижения:

✅ **Сложные схемы**: 37 таблиц в questsH обработаны за 5 минут  
✅ **Графовые данные**: рекурсивные CTE и анализ путей оптимизированы  
✅ **OLAP архитектура**: звездообразные схемы с оконными функциями  
✅ **Реальные данные**: подключение к БД для статистики (row_count, cardinality)

## 🔧 Особенности реализации

### Реальные оптимизации SQL

```python
# ❌ Плохо: комментарии вместо изменений
SELECT * FROM table -- TODO: add LIMIT

# ✅ Хорошо: реальные изменения через sqlglot
SELECT col1, col2, col3 FROM table LIMIT 10000
```

**Применяемые оптимизации:**
- **Column pruning**: замена `SELECT *` на конкретные колонки
- **Automatic LIMIT**: добавление ограничений для неагрегационных запросов
- **Partition pruning**: использование партиционных колонок в WHERE
- **Clustered joins**: JOIN по кластерным колонкам

### Подключение к базам данных

Система поддерживает получение реальной статистики из:
- **Trino/Presto**: количество строк, размер, партиции
- **PostgreSQL**: метаданные таблиц
- **MySQL**: структура данных

```python
# Получаем статистику для анализа
table_stats = {
    "users": {
        "row_count": 1_000_000,
        "size_bytes": 524_288_000,
        "column_stats": {
            "user_id": {"cardinality": 1000000, "distinct_count": 1000000},
            "country": {"cardinality": 195, "distinct_count": 195}
        }
    }
}
```

### Эвристический анализ

Если LLM недоступен, система использует интеллектуальную эвристику:

**Партиционирование** (до 2 колонок):
- DATE/TIMESTAMP колонки с высокой кардинальностью
- Колонки с ключевыми словами: date, time, created, updated

**Кластеризация** (до 4 колонок):
- ID колонки со средней кардинальностью (10 < card < 50% строк)
- Колонки с ключевыми словами: id, key, code, type, status

### Ротация логов

Автоматическое управление файлами логов:
- **Интервал**: каждый час
- **Хранение**: 24 последних файла
- **Формат**: `sql_agent_YYYYMMDD_HHMMSS.log`
- **Автоочистка**: удаление устаревших файлов

```python
# Информация о логах через /stats
{
  "log_info": {
    "log_directory": "logs",
    "current_log_file": "logs/sql_agent_20251001_143022.log",
    "total_files": 15,
    "total_size_bytes": 5242880
  }
}
```

## 📁 Структура проекта

```
sql-agent/
├── sql_agent/
│   ├── __init__.py              # Инициализация пакета
│   ├── api.py                   # FastAPI endpoints
│   ├── models.py                # Pydantic модели
│   ├── task_manager.py          # Управление задачами
│   ├── llm_analyzer.py          # Гибридный анализатор
│   ├── db_connector.py          # Подключение к БД
│   └── log_rotator.py           # Ротация логов
│
├── datasets/                    # Тестовые данные
│   ├── flights.json             # 1 таблица, 20 запросов
│   ├── questsH.json             # 37 таблиц, 10 запросов
│   ├── linear_schema.json       # 4 таблицы (линейная схема)
│   ├── star_schema.json         # 5 таблиц (OLAP схема)
│   └── network_schema.json      # 5 таблиц (графовые данные)
│
├── main.py                      # Точка входа
├── test.py                      # Система тестирования
├── requirements.txt             # Зависимости
├── env_example.txt              # Пример конфигурации
└── README.md                    # Документация
```

## 🔬 Эксперименты с моделями

Проведено тестирование **27 различных LLM моделей** для анализа БД.

### Топ-5 моделей (8-9 баллов):

1. **nvidia/nemotron-nano-9b-v2** ⭐ — выбрана для продакшена
2. **qwen/qwen3-8b** — отличная альтернатива
3. **google/gemini-2.5-flash-preview-09-2025** — используется для оценки
4. **mistralai/mistral-7b-instruct** — стабильные результаты
5. **anthropic/claude-sonnet-4** — высокое качество

### Критерии оценки:
- Корректность SQL синтаксиса
- Полнота рекомендаций
- Валидность путей к таблицам
- Практичность оптимизаций
- Соответствие требованиям Iceberg

## ⚙️ Конфигурация

### Переменные окружения (.env):

```bash
# OpenRouter API ключ (обязательно)
OPEN_ROUTER=your_api_key_here

# Опционально: кастомный URL
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
```

### Настройки task_manager:

```python
task_manager = SimpleTaskManager(
    max_workers=4,              # Параллельные задачи
    task_timeout_minutes=20,    # Таймаут задачи
    use_llm=True               # Использовать LLM
)
```

### Настройки LLM:

```python
llm_analyzer = LLMAnalyzer(
    max_workers=6,                    # Потоки для запросов
    api_key="your_key",               # API ключ
    base_url="https://openrouter.ai"  # URL провайдера
)
```

## 🚀 Развертывание

### Локальный запуск:

```bash
python main.py
# Сервер: http://localhost:8001
```

### Docker (будущая версия):

```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "main.py"]
```

### Production рекомендации:
- Используйте Gunicorn/Uvicorn с несколькими воркерами
- Настройте reverse proxy (Nginx)
- Включите мониторинг и алерты
- Настройте ротацию логов
- Используйте переменные окружения для секретов

## 🧪 Тестирование

### Запуск всех тестов:

```bash
python test.py
```

### Выбор конкретного датасета:

```bash
python test.py --dataset flights
```

### Параллельное тестирование:

```bash
# Запускает 3 задачи одновременно
python test.py --concurrent
```

### Результаты тестов:

```
📊 Результаты тестирования:
   ✅ Успешно: 5/5
   ❌ Ошибок: 0
   ⏱️  Среднее время: 164.00s
   📈 Средняя оценка: 88.80/100
```

## 📝 Примеры использования

### 1. Создание задачи

```python
import requests

response = requests.post("http://localhost:8001/new", json={
    "url": "jdbc:trino://localhost:8080?catalog=test",
    "ddl": [
        {"statement": "CREATE TABLE users (id INTEGER, name VARCHAR)"}
    ],
    "queries": [
        {
            "queryid": "q1",
            "query": "SELECT * FROM users",
            "runquantity": 100
        }
    ]
})

task_id = response.json()["taskid"]
```

### 2. Проверка статуса

```python
status = requests.get(
    f"http://localhost:8001/status?task_id={task_id}"
).json()

print(status["status"])  # RUNNING, DONE, или FAILED
```

### 3. Получение результата

```python
result = requests.get(
    f"http://localhost:8001/getresult?task_id={task_id}"
).json()

print(f"DDL statements: {len(result['ddl'])}")
print(f"Migrations: {len(result['migrations'])}")
print(f"Quality score: {result['quality_score']}/100")
```

## ⚠️ Ограничения

### Текущие ограничения:

1. **API зависимость** — требуется OPEN_ROUTER ключ для работы LLM
2. **Таймауты** — задачи ограничены 20 минутами
3. **Поддержка БД** — Trino, PostgreSQL, MySQL (другие в планах)
4. **JSON парсинг** — иногда требуется repair попытка для LLM ответов
5. **Размер схемы** — очень большие схемы (>100 таблиц) могут требовать разбиения

### Известные проблемы:

- Некоторые LLM модели возвращают некорректный JSON (используется repair механизм)
- Подключение к БД опционально, но без него статистика недоступна
- Очень сложные рекурсивные запросы могут потребовать ручной проверки

## 🔄 История версий

### v1.4 (текущая) - Качество кода
- ✅ Соответствие PEP8
- ✅ Подробные docstring'и
- ✅ Удалены неиспользуемые импорты
- ✅ Исправлены баги форматирования

### v1.3 - Подключение к БД
- ✅ DatabaseConnector для Trino/PostgreSQL/MySQL
- ✅ Реальная статистика данных (row_count, cardinality)
- ✅ Эвристический анализ на основе данных
- ✅ Улучшенная генерация партиций и кластеров

### v1.2 - Переход на Nemotron
- ✅ nvidia/nemotron-nano-9b-v2 как основная модель
- ✅ Строгий JSON контроль без fallback
- ✅ Новые тестовые схемы (linear, star, network)
- ✅ Увеличен max_tokens до 12000

### v1.1 - Оптимизация промптов
- ✅ Сокращение промптов (73→30 строк)
- ✅ Улучшенная модель оценки
- ✅ Таймаут 20 минут

### v1.0 - Базовая версия
- ✅ Интеграция с LLM
- ✅ Асинхронная обработка
- ✅ REST API
- ✅ Система тестирования

## 🎯 Roadmap

### Ближайшие планы:
- [ ] Поддержка ClickHouse и других OLAP БД
- [ ] Кэширование результатов анализа
- [ ] Веб-интерфейс для мониторинга
- [ ] Экспорт отчетов в PDF/HTML
- [ ] Интеграция с CI/CD для автоматического анализа

### Долгосрочные цели:
- [ ] Поддержка NoSQL баз данных
- [ ] ML модели для предсказания производительности
- [ ] Автоматическое применение миграций
- [ ] Интеграция с системами мониторинга (Grafana, Prometheus)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в директории `logs/`
2. Проверьте статус через `/health` endpoint
3. Убедитесь, что API ключ OPEN_ROUTER настроен корректно
4. Изучите примеры в `test.py`

## 📄 Лицензия

MIT License - свободное использование для любых целей.

---

**SQL-agent** — готовое решение для автоматической оптимизации баз данных с использованием современных LLM технологий! 🚀